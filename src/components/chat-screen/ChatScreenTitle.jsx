import React from "react";

const ChatScreenTitle = ({ title }) => (
  <div className="chat-screen-title" style={{ backgroundColor: "skyblue" }}>
    {title}
  </div>
);

export default ChatScreenTitle;
